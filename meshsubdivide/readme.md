# SMPL Mesh Subdivision and High-Resolution Model Generation

# ------meshsubdivide.py------

本文件用于将 **SMPL 模型**（原始约 6890 顶点）细分为高分辨率版本，并同步更新其参数模型组件（`J_regressor`, `shapedirs`, `posedirs`, `weights`, `J_regressor_extra`），从而得到兼容高分辨率网格的 SMPL 模型。

---

## 📘 功能概述

该脚本实现了以下流程：

1. **网格细分**  
   将输入的三角形网格（SMPL 模板）进行 Loop-style 子面细分，每个三角形细分为 4 个。
   
2. **顶点来源追踪（origin_pairs）**  
   为每个新顶点记录其来源的原始顶点索引对 `(v0, v1)`：
   - 原始顶点：`(i, i)`
   - 新生成中点：`(a, b)`

3. **参数同步插值**  
   根据 `origin_pairs` 对以下参数进行插值与扩展：
   - `J_regressor`（关节回归矩阵）
   - `J_regressor_extra`（额外关节，如脚趾等）
   - `shapedirs`（形状基）
   - `posedirs`（姿态基）
   - `weights`（LBS蒙皮权重）

4. **生成高分辨率 SMPL 模型文件**
   输出新的 `.pkl` 模型与 `.npy` 文件，可直接替换或用于 SMPL 拓展实验。

---

## 主要函数说明

| 函数名 | 功能 | 输入 | 输出 |
|--------|------|------|------|
| `subdivide_with_origin()` | 执行 mesh 细分并记录每个顶点来源 | 顶点、面 | 细分后的顶点、面、`origin_pairs` |
| `compute_high_res_J_regressor()` | 生成高分辨率 `J_regressor` | 原始回归矩阵、`origin_pairs` | 稀疏矩阵 |
| `compute_high_res_shapedirs()` | 生成高分辨率形状基 | 原始 `shapedirs`、`origin_pairs` | Chumpy 张量 |
| `compute_high_res_posedirs()` | 生成高分辨率姿态基 | 原始 `posedirs`、`origin_pairs` | ndarray |
| `compute_high__weights()` | 插值生成高分辨率蒙皮权重 | 原始权重、`origin_pairs` | ndarray |
| `compute_high_subdivide_J_extra()` | 生成高分辨率脚趾等附加关节权重 | 原始 `J_regressor_extra`、`origin_pairs` | ndarray |

# ------model_test.py------

本文件用于将生成的**细分模型**进行测试。需要修改**util.config**的路径进行修改。

# ------meshfitting.py------

本文件用于用smpl模型以加d的方式拟合一个参数模型。